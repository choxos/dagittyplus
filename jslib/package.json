{
  "name": "dagitty-engine",
  "version": "0.4.0",
  "private": true,
  "description": "Node build of the DAGitty causal graph engine; provides the runtime dependency that the generated dagitty-node.js bundle resolves.",
  "dependencies": {
    "underscore": "^1.13.8"
  }
}
